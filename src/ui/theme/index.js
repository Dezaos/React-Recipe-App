import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#42A5F5" },
  secondary: { main: "#F44336" },
};
const themeName = "Picton Blue Pomegranate Civet";

export default createMuiTheme({ palette, themeName });
